---
title: "인공 신경망의 활성화 함수(Activation Function) 개념과 종류"
date: 2026-07-12T00:00:00+09:00
tags: ["알고리즘", "핵토200", "딥러닝", "활성화함수"]
topic_no1: 22
topic_no2: 1
topic_large: "활성화함수"
topic_small: "활성화함수"
exam_ref: "모의_2019.11"
exam_type: "관리"
question_no: 3
---

## 문제

인공 신경망에서 사용하는 활성화 함수(activation function)에 대하여 다음을 설명하시오.
가. 활성화 함수 개념 및 역할
나. 활성화 함수 종류

## 출제 정보

| 항목 | 내용 |
|------|------|
| 토픽(대) | 활성화함수 |
| 토픽(소) | 활성화함수 |
| 출제 | 모의_2019.11 |
| 유형 | 관리 |
| 번호 | 3 |

## 모범답안

1. 가. 활성화 함수 개념 및 역할

    정 의  • 뉴런의 가중합(Weighted Sum) 출력에 비선형 변환을 적용하여
             신경망이 복잡한 비선형 패턴을 학습할 수 있게 하는 함수
           - 활성화 함수가 없으면 층을 깊게 쌓아도 단순 선형변환 조합에 불과

    ```
    입력(x) → 가중합 z = Σ(w·x)+b → 활성화 함수 f(z) → 출력(다음 층으로 전달)

    [활성화 함수 없을 경우]                 [활성화 함수 있을 경우]
     f(x) = Wn·...·W2·W1·x+b                f(x) = σ(Wn·σ(...σ(W1·x+b1)...)+bn)
     = 단일 선형변환으로 축약(표현력 상실)    = 비선형 조합으로 복잡한 함수 근사 가능
    ```

    | 역할 | 설명 |
    |------|------|
    | 비선형성 도입 | 층을 깊게 쌓아도 표현력이 증가하도록 보장 |
    | 출력 범위 제어 | 값의 범위를 제한(0~1, -1~1 등)해 학습 안정화 |
    | 기울기 전달 | 역전파 시 미분값을 통해 가중치 갱신 신호 전달 |
    | 희소성 유도 | ReLU 등은 음수 입력을 0으로 만들어 희소 표현 형성 |

    - 활성화 함수는 딥러닝이 "깊이"를 통해 표현력을 얻는 근본 원리.

2. 나. 활성화 함수 종류

    | 함수 | 수식 | 미분(도함수) | 출력 범위 |
    |------|------|--------------|-----------|
    | Sigmoid | σ(x)=1/(1+e^-x) | σ(x)(1-σ(x)) | (0, 1) |
    | Tanh | tanh(x)=(e^x-e^-x)/(e^x+e^-x) | 1-tanh²(x) | (-1, 1) |
    | ReLU | f(x)=max(0,x) | x>0: 1, x≤0: 0 | [0, ∞) |
    | Leaky ReLU | x>0: x, x≤0: αx (α≈0.01) | x>0: 1, x≤0: α | (-∞, ∞) |
    | GELU | x·Φ(x) (Φ: 표준정규 누적분포함수) | 근사식, 매끄러운 곡선 | (-∞, ∞) 근사 |

    - 활성화 함수 선택은 기울기 소실 문제와 직결, Sigmoid/Tanh → ReLU → GELU 순으로 발전.

3. 활성화 함수 비교 및 발전 계보

    ```
    Sigmoid/Tanh (초기, 기울기 소실 취약)
          ↓
       ReLU (2012~, 계산 단순·기울기 소실 완화, but Dying ReLU)
          ↓
    Leaky ReLU / PReLU / ELU (Dying ReLU 보완)
          ↓
       GELU / Swish (2018~, Transformer 계열 표준, 매끄러운 비선형)
    ```

    | 구분 | Sigmoid | Tanh | ReLU | GELU |
    |------|---------|------|------|------|
    | 기울기 소실 | 심함(양끝 포화) | 있음(양끝 포화) | 양수 구간 없음 | 거의 없음 |
    | 계산 비용 | 지수 연산 필요 | 지수 연산 필요 | 매우 낮음 | 상대적으로 높음 |
    | 주요 문제 | Vanishing Gradient | Vanishing Gradient(완화) | Dying ReLU | 문제 적음, 연산량 소폭 증가 |
    | 주요 사용처 | 이진분류 출력층 | RNN 은닉층(과거) | CNN 은닉층 표준 | Transformer(BERT, GPT 등) |

    - 은닉층은 ReLU 계열이 기본값, 최신 대규모 모델은 매끄러운 GELU/Swish가 표준.
    - 출력층은 이진분류 Sigmoid, 다중분류 Softmax, 회귀는 항등함수(Identity) 사용.

4. 실무 적용 시 고려사항

    | 이슈 | 대응방안 |
    |------|----------|
    | 학습률·초기화 부적절 시 다수 뉴런이 죽는 Dying ReLU | He 초기화 병행, 활성화 값 분포 모니터링 후 Leaky ReLU/PReLU 전환 |
    | 소규모 모델에 GELU/Swish 무분별 적용 시 연산 대비 이득 미미 | 경량 모델은 ReLU/Leaky ReLU 우선, 대규모 Transformer만 벤치마크 후 채택 |
    | 정규화 층과 활성화 함수 조합에 따른 안정성 편차 | 정규화 위치(활성화 전/후)와 함수 종류를 함께 실험해 최적 조합 결정 |

    - 최근 Transformer 계열 확산으로 GELU/Swish 등 매끄러운 활성화 함수 적용 증대 예상.  "끝"
