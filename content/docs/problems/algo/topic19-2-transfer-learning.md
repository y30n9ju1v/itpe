---
title: "전이학습(Transfer Learning)"
date: 2026-07-12T00:00:00+09:00
tags: ["알고리즘", "핵토200", "모델활용", "모델활용"]
topic_no1: 19
topic_no2: 2
topic_large: "모델활용"
topic_small: "모델활용"
exam_ref: "131회"
exam_type: "응용"
question_no: 1
---

## 문제

전이학습(Transfer Learning)

## 출제 정보

| 항목 | 내용 |
|------|------|
| 토픽(대) | 모델활용 |
| 토픽(소) | 모델활용 |
| 출제 | 131회 |
| 유형 | 응용 |
| 번호 | 1 |

## 모범답안

### 1. 전이학습(Transfer Learning)의 정의

정 의  • 특정 도메인/태스크(Source Task)에서 학습된 모델의 지식(가중치)을
       관련성 있는 다른 도메인/태스크(Target Task)에 재사용하여
       적은 데이터와 자원으로 높은 성능을 달성하는 학습 기법
      - "밑바닥부터 학습하지 않고, 이미 배운 지식을 응용한다"

```
[Source Task: 대규모 데이터(예: ImageNet 1400만장)]
        ↓ Pre-training (범용 특징 학습: 엣지, 텍스처, 형태 등)
   [사전학습 모델(Pre-trained Model)]
        ↓ 가중치 이전(Weight Transfer)
[Target Task: 소량 데이터(예: 특정 질병 X-ray 500장)]
        ↓ Fine-tuning (일부 계층만 재학습)
   [목표 태스크에 특화된 모델]
```

### 2. 전이학습 전략 비교

| 구분 | Feature Extraction | Fine-tuning |
|------|---------------------|--------------|
| 방법 | 사전학습 계층 고정(Freeze), 출력층만 새로 학습 | 사전학습 가중치를 초기값으로 전체(일부) 재학습 |
| 목표 데이터 양 | 매우 적을 때 유리 | 어느 정도 확보되었을 때 유리 |
| 학습 비용 | 낮음(출력층만 학습) | 상대적으로 높음(다수 계층 학습) |
| 과적합 위험 | 낮음 | 목표 데이터 적으면 위험 증가 |

```
CNN 계층별 전이학습 적용 전략:
  하위 계층(엣지, 색상 등 범용 특징) → 고정(Freeze) 유지
  상위 계층(도메인 특화 고차 특징)   → 재학습(Fine-tuning) 대상
```

### 3. 적용 판단 기준 및 활용/트렌드

| 데이터량 \ 도메인 유사도 | 유사도 높음 | 유사도 낮음 |
|--------------------------|-------------|-------------|
| 목표 데이터 적음 | Feature Extraction | 전이학습 효과 제한적, 신중 적용 |
| 목표 데이터 많음 | 전체 Fine-tuning | 사전학습 가중치를 초기값 삼아 전체 재학습 |

| 활용사례 | 설명 |
|----------|------|
| 이미지 | ImageNet 사전학습 ResNet/VGG를 의료영상 진단에 전이 |
| 자연어처리 | BERT/GPT를 특정 도메인 챗봇으로 파인튜닝 |
| 음성 | 범용 음성인식 모델을 특정 억양/전문용어에 특화 |

- 도메인 유사도 낮을 때 무분별한 재사용은 부정적 전이(Negative Transfer) 유발 위험, 최근 LLM 시대 LoRA·Prompt Tuning 등 PEFT 기법 적용 증대.  "끝"
