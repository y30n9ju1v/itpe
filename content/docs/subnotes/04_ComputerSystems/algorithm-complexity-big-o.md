---
title: "알고리즘 복잡도와 O-Notation"
date: 2026-07-11T11:37:56+09:00
tags: ["컴퓨터시스템", "알고리즘", "복잡도", "O표기법", "정렬알고리즘", "서브노트"]
draft: false
---

# 알고리즘 복잡도와 O-Notation 서브노트

> **두음 머리에 박기 🧠**
> - **오·오메가·세타** (점근 표기법 3종: **O**(Big-O, 최악 상한), **Ω**(Big-Omega, 최선 하한), **Θ**(Big-Theta, 평균/상하한 동일))
> - **1·로그·엔·엔로그엔·엔제곱·엔세제곱·이엔·엔팩토리얼** (복잡도 성능 순서: O(1) < O(log n) < O(n) < O(n log n) < O(n²) < O(n³) < O(2ⁿ) < O(n!))

---

| 항목 | 내용 |
| :--- | :--- |
| **토픽명** | **알고리즘 복잡도 (시간·공간복잡도) 및 O-Notation** |
| **정의** | 입력 크기 n에 따른 연산횟수(시간복잡도)·메모리사용량(공간복잡도)의 수학적 표현 + 최악 성능 상한 표기법 **O-Notation(Big-O)** |
| **키워드** | O/Ω/Θ, 시간복잡도/공간복잡도, 정렬 알고리즘 복잡도, 안정성(Stable), 분할 정복 |
| **개념도** | `성능 우수 ← O(1) < O(log n) < O(n) < O(n log n) < O(n²) < O(n³) < O(2ⁿ) < O(n!) → 성능 저하`<br>`[ 예시 ] O(1): 해시 조회 / O(log n): 이진탐색 / O(n log n): 퀵·병합정렬(평균) / O(n²): 버블·삽입정렬 / O(2ⁿ): 완전탐색` |
| **구성요소** | 1. **시간복잡도**: 입력 크기 → 비교·대입 연산횟수 증가 양상<br>2. **공간복잡도**: 입력 크기 → 메모리(변수·자료구조) 사용량 증가 양상<br>3. **O-Notation 3종**: O(최악 상한, 실무표준) / Ω(최선 하한) / Θ(평균, 상하한 일치)<br>4. **복잡도 계산 규칙**: 상수 제거(O(3n)→O(n)) → 저차항 제거(O(n²+n)→O(n²)) → 최고차항만 유지 |
| **비교** | **퀵 정렬 (Quick Sort)**<br>- **평균 시간복잡도**: O(n log n)<br>- **최악 시간복잡도**: O(n²) (이미 정렬된 배열에서 피벗 선택 불량 시)<br>- **안정성**: 불안정(Unstable)<br>- **공간복잡도**: O(log n)<br><br>**병합 정렬 (Merge Sort)**<br>- **평균 시간복잡도**: O(n log n)<br>- **최악 시간복잡도**: O(n log n) (항상 일정)<br>- **안정성**: 안정(Stable)<br>- **공간복잡도**: O(n) (추가 배열 필요) |
| **차별화** | **복잡도 분석 실무 답안 전략**<br>1. **최악/최선/평균 구분 실무 의미**: 실시간시스템 SLA → O(최악) 설계기준, 최악 시나리오(퀵정렬 O(n²)) 우려 시 병합정렬 등 최악보장 알고리즘 대안<br>2. **안정정렬 필요 상황**: 동일키 원래순서 보존 다중필드 정렬(이름순→부서순 재정렬) → 안정정렬(병합·삽입·버블) 선택<br>3. **지수/팩토리얼 복잡도 회피**: 배낭문제·TSP 완전탐색 O(2ⁿ)·O(n!) → DP·근사알고리즘·메타휴리스틱(유전알고리즘)으로 다항시간 근사해 |
